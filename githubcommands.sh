#/bin/bash

#series of commands to push changes made locally to server
git add --all
git commit
git push origin master

#series of commands to download changes made on server
#git fetch upstream
#git checkout master
#git merge upstream/master

#list updates about local directory
#git branch -v

#list remote directories attached to local directory
#git remote -v
